<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.application.foodhub.postLike.PostLikeDAO">

	<delete id="deletePostLike">
	    DELETE 	FROM POST_LIKE 
	    WHERE 	POST_ID = #{postId} 
	    AND 	USER_ID = #{userId}
	</delete>
	
	<insert id="insertPostLike">
	    INSERT INTO POST_LIKE 
	    		(POST_ID, 
	    		USER_ID) 
   		VALUES (#{postId}, 
   				#{userId})
	</insert>
	
	<select id="existsPostLike" resultType="boolean">
	    SELECT EXISTS (
	        SELECT 1 FROM POST_LIKE WHERE POST_ID = #{postId} AND USER_ID = #{userId}
	    )
	</select>
	
	<select id="countPostLikes" resultType="int">
	    SELECT 	COUNT(*) 
	    FROM 	POST_LIKE 
	    WHERE 	POST_ID = #{postId}
	</select>
	
</mapper>